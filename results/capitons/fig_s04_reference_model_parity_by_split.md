# fig_s04_reference_model_parity_by_split

Observed versus predicted original TDC target for the validation-selected reference model, separately for each of five Primary split runs. Each panel contains the same 182 fixed-test records and uses identical numerical limits.
