# fig_04_validation_vs_primary

Each symbol represents one frozen baseline (n=16); color denotes molecular representation and shape denotes algorithm. The dashed identity line is descriptive. Validation and fixed-test errors were evaluated on different partitions, so equality is not expected.
