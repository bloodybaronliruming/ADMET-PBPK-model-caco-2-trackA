# fig_07_performance_by_target_range

Quartile cutoffs were computed from the 728-row train/validation pool before application to fixed test. Each small point is one Primary split model's test MAE within a target bin; large points show mean ± sample SD across five split runs. Counts refer to distinct fixed-test records. These are data-defined ranges, not pharmacological permeability classes.
